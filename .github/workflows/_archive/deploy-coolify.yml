name: Deploy via Coolify
# Dev deployment to IONOS Dev server via Coolify Cloud API.
# Updates image tag + triggers Coolify restart.
# All runtime secrets are injected by Coolify via Doppler-synced environment variables.
#
# Required Doppler config (behind DOPPLER_TOKEN_COOLIFY → kombify-ionos-prod/prd):
#   COOLIFY_API_TOKEN        — Coolify Cloud API token
#   <resource-uuid-key>      — Coolify resource UUID per service (e.g. COOLIFY_CLOUD_RESOURCE_UUID)

on:
  workflow_call:
    inputs:
      service-name:
        description: "Service name for logging"
        required: true
        type: string
      resource-uuid-key:
        description: "Doppler key name that holds the Coolify resource UUID"
        required: true
        type: string
      image-tag:
        description: "Docker image tag to deploy"
        required: false
        type: string
        default: "latest"
      image-repository:
        description: "Full image repository (e.g. ghcr.io/kombiverselabs/kombify-cloud)"
        required: false
        type: string
      health-check-url:
        description: "URL to probe after deployment"
        required: false
        type: string
      health-check-retries:
        description: "Number of health check retries"
        required: false
        type: number
        default: 8
      health-check-interval:
        description: "Seconds between health check retries"
        required: false
        type: number
        default: 10
      force:
        description: "Force redeploy even if same tag"
        required: false
        type: boolean
        default: false
      runner:
        description: "Runner label"
        required: false
        type: string
        default: "blacksmith-4vcpu-ubuntu-2204"
    secrets:
      DOPPLER_TOKEN_COOLIFY:
        description: "Doppler service token for kombify-ionos-prod/prd (resolves COOLIFY_API_TOKEN + resource UUIDs)"
        required: true

jobs:
  deploy:
    name: "${{ inputs.service-name }} → Dev (Coolify)"
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 10
    steps:
      - name: Install Doppler CLI
        shell: bash
        run: |
          if command -v doppler >/dev/null 2>&1; then exit 0; fi
          sudo mkdir -p /usr/share/keyrings
          if [ ! -f /usr/share/keyrings/doppler-archive-keyring.gpg ]; then
            curl -sLf --retry 3 --tlsv1.2 --proto "=https" \
              'https://packages.doppler.com/public/cli/gpg.DE2A7741A397C129.key' \
              | sudo gpg --batch --yes --dearmor -o /usr/share/keyrings/doppler-archive-keyring.gpg
          fi
          if [ ! -f /etc/apt/sources.list.d/doppler-cli.list ]; then
            echo "deb [signed-by=/usr/share/keyrings/doppler-archive-keyring.gpg] https://packages.doppler.com/public/cli/deb/debian any-version main" \
              | sudo tee /etc/apt/sources.list.d/doppler-cli.list
          fi
          sudo apt-get update -qq && sudo apt-get install -y doppler

      - name: Resolve Coolify credentials
        id: resolve
        shell: bash
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_COOLIFY }}
        run: |
          COOLIFY_TOKEN=$(doppler secrets get COOLIFY_API_TOKEN --plain)
          echo "::add-mask::$COOLIFY_TOKEN"
          echo "coolify_token=$COOLIFY_TOKEN" >> "$GITHUB_OUTPUT"

          UUID=$(doppler secrets get "${{ inputs.resource-uuid-key }}" --plain)
          echo "uuid=$UUID" >> "$GITHUB_OUTPUT"
          echo "Resolved UUID for ${{ inputs.service-name }} from key: ${{ inputs.resource-uuid-key }}"

      - name: Validate Coolify resource
        shell: bash
        env:
          COOLIFY_TOKEN: ${{ steps.resolve.outputs.coolify_token }}
        run: |
          UUID="${{ steps.resolve.outputs.uuid }}"
          API_URL="https://app.coolify.io/api/v1/applications/${UUID}"

          echo "Checking resource: $API_URL"
          RESPONSE=$(curl -sf -H "Authorization: Bearer $COOLIFY_TOKEN" "$API_URL" 2>/dev/null) || {
            echo "::error::Failed to reach Coolify API or resource not found (UUID: $UUID)"
            exit 1
          }

          STATUS=$(echo "$RESPONSE" | jq -r '.status // "unknown"')
          echo "Resource status: $STATUS"

          if [[ "$STATUS" == exited* ]]; then
            echo "::error::Resource is in exited state. Fix manually before deploying."
            exit 1
          fi

      - name: Update image tag
        shell: bash
        env:
          COOLIFY_TOKEN: ${{ steps.resolve.outputs.coolify_token }}
        run: |
          UUID="${{ steps.resolve.outputs.uuid }}"
          REPO="${{ inputs.image-repository }}"
          TAG="${{ inputs.image-tag }}"

          echo "Updating application $UUID to image ${REPO:+$REPO:}${TAG}"

          PAYLOAD="{\"docker_registry_image_tag\":\"${TAG}\""
          if [ -n "$REPO" ]; then
            PAYLOAD="${PAYLOAD},\"docker_registry_image_name\":\"${REPO}\""
          fi
          PAYLOAD="${PAYLOAD}}"

          curl -sf -X PATCH \
            -H "Authorization: Bearer $COOLIFY_TOKEN" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" \
            "https://app.coolify.io/api/v1/applications/${UUID}" > /dev/null

          echo "Image tag updated"

      - name: Trigger deployment
        shell: bash
        env:
          COOLIFY_TOKEN: ${{ steps.resolve.outputs.coolify_token }}
        run: |
          UUID="${{ steps.resolve.outputs.uuid }}"
          FORCE="${{ inputs.force }}"

          echo "Triggering deployment for ${{ inputs.service-name }} (force=$FORCE)..."
          curl -sf -X POST \
            -H "Authorization: Bearer $COOLIFY_TOKEN" \
            "https://app.coolify.io/api/v1/deploy?uuid=${UUID}&force=${FORCE}" > /dev/null

          echo "Deployment triggered"

      - name: Wait for container startup
        shell: bash
        run: |
          echo "Waiting 30s for container to start..."
          sleep 30

      - name: Health check
        if: inputs.health-check-url != ''
        shell: bash
        run: |
          URL="${{ inputs.health-check-url }}"
          RETRIES=${{ inputs.health-check-retries }}
          INTERVAL=${{ inputs.health-check-interval }}

          echo "Health checking: $URL (max ${RETRIES} attempts, ${INTERVAL}s interval)"

          for i in $(seq 1 "$RETRIES"); do
            HTTP_CODE=$(curl -so /dev/null -w '%{http_code}' --max-time 10 "$URL" 2>/dev/null || echo "000")
            echo "  Attempt $i/$RETRIES: HTTP $HTTP_CODE"

            if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 400 ]; then
              echo "Health check passed"
              exit 0
            fi

            if [ "$i" -lt "$RETRIES" ]; then
              sleep "$INTERVAL"
            fi
          done

          echo "::error::Health check failed after $RETRIES attempts at $URL"
          exit 1
