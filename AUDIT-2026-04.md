# .github - Platform Audit April 2026

## Status: REMEDIATED FOR RUNNER MIGRATION

This note replaces the accidentally deleted local audit artifact.
It reflects the state after the Blacksmith migration work completed on 2026-04-09.

---

## What Was Previously Flagged

The earlier audit identified these runner- and workflow-related issues as the highest-value items:

1. `aspire-integration-gate.yml` still referenced the archived `pick-runner.yml`
2. `deploy-render-staged.yml` accepted `image-tag` / `image-repository` inputs but ignored them
3. `ghcr-cleanup.yml` still used `ubuntu-latest`
4. `CLAUDE.md` had stale runner documentation

---

## What Is Implemented Now

### Runner Selection

- `aspire-integration-gate.yml` now uses `pick-runner-v2.yml`
- `ghcr-cleanup.yml` now runs on `${{ vars.CI_RUNNER || 'blacksmith-4vcpu-ubuntu-2204' }}`
- Shared workflows were pushed to GitHub and verified against the live repo state

### Render Staged Deploy

- `deploy-render-staged.yml` now includes `imageUrl` in the Render deploy payload when `image-repository` is provided
- This removes the earlier drift where inputs existed but were operationally ignored

### Documentation

- `CLAUDE.md` was updated to reflect the current Blacksmith defaults and deployment topology
- Legacy `deploy-coolify.yml` was moved into `_archive/`

---

## Verification Performed

The published GitHub repo was checked directly after push.

Relevant workflows verified on `KombiverseLabs/.github`:

- `ghcr-cleanup.yml`: no GitHub-hosted runner labels remain
- `aspire-integration-gate.yml`: no GitHub-hosted runner labels remain
- `deploy-render-staged.yml`: no GitHub-hosted runner labels remain

---

## Remaining Notes

- This file only re-documents the runner-migration and shared-workflow fixes that were actually verified.
- It is **not** a full fresh audit of every non-runner concern from the original report.
- If you still want the broader maintenance topics rechecked, that should be done as a separate audit pass.

---

## Verdict

For the specific migration target of this session, the `.github` repo is in the correct state:

- shared workflow runner drift fixed
- staged Render deploy input drift fixed
- live GitHub state verified

Anything beyond that should not be inferred from this note.